export enum UserStatus {
  Enabled = "Enabled",
  Disabled = "Disabled",
}

export enum ArticleType {
  HTML = "HTML",
  MarkDown = "MD",
}
export enum ArticleStatus {
  Draft = "Draft",
  Published = "Published",
  Deleted = "Deleted",
}
